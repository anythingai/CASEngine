# Cultural Arbitrage API - Local Dev Environment (.env.local)
# NOTE:
# - Copy this file to: apps/api/.env.local (DO NOT COMMIT the .env.local file)
# - These values are for local development parity with Azure.
# - The API reads these server-side envs; the frontend does not need server secrets.

# ------------------------------
# Server runtime (local dev)
# ------------------------------
NODE_ENV=development
PORT=8000

# Local dev runs Next.js dev server at :3000; the API redirects non-API routes there
SERVE_STATIC_FRONTEND=false
FRONTEND_URL=http://localhost:3000

# Logging / CORS / Limits
LOG_LEVEL=debug
ENABLE_CORS=true
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

ENABLE_RATE_LIMITING=false
MAX_REQUEST_SIZE=10mb
REQUEST_TIMEOUT=30000
HEALTH_CHECK_INTERVAL=30000

# Auth (generate your own strong secrets locally)
JWT_SECRET=change-this-in-local-dev
API_KEY=local-dev-api-key

# ------------------------------
# Azure OpenAI (required for real analysis)
# Use the same values you set in Azure App Settings
# ------------------------------
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=  # e.g., https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=o4-mini
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# ------------------------------
# Optional external APIs
# ------------------------------
COINGECKO_API_KEY=
OPENSEA_API_KEY=
FARCASTER_API_KEY=
QLOO_API_KEY=
QLOO_API_URL=

# ------------------------------
# Cache TTLs
# ------------------------------
CACHE_TTL_SHORT=300
CACHE_TTL_MEDIUM=1800
CACHE_TTL_LONG=3600

# ------------------------------
# Notes
# ------------------------------
# - Do NOT set NEXT_PUBLIC_ secrets here; those are client-visible.
# - Keep this file out of version control (git-ignored).
# - For Azure single-container production:
#     SERVE_STATIC_FRONTEND=true (set via Azure App Settings; not used locally)